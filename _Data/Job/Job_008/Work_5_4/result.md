# [파일_이미지_리사이즈_실행] "Banana Unity 임포트 높이 보정" 업무 레포트

## 요약

- 선택 원본 `3.png`의 임시 사본을 `image_normalize`로 정규화해 PNG `256x256`, 알파 바운딩 박스 높이 `124px` 후보를 확보했다.
- 원본 pool·Asset·`.meta`는 변경하지 않았으며 후보는 후속 `Work_5_5` 입력으로 보존돼 있다.

## 완료업무

### Banana 리사이즈 후보 생성
**산출물**
`_Temp/Job_008_Work_5_4_Banana_Move_candidate.png`
**작업내용**
- 입력 원본 실측은 `256x256`, 알파 바운딩 박스 `(67,61,209,184)`, 높이 `123px`다.
- `image_normalize` `Work_0024` 완료 결과는 `256x256`, 알파 바운딩 박스 `(56,66,200,190)`, 높이 `124px`다.
- 프로젝트 밖 임시 파일이라 Unity `.meta`·프리팹 사용처 검사는 대상이 아니다.
