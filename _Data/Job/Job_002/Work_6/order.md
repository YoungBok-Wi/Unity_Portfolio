# 업무지시서

## 1. 씬 구성

**대상 스킬**: 유니티엔진_씬_구성

**"taskContent"**: `Scene_Game` 카메라 비율 교정·디버그 콘솔 숨김과 두 씬의 컨셉아트 대조

**업무**

- 근거: `_Data/Job/Job_001/Work_6/result.md` `## 비고` — 캐릭터 화면 높이 7.7%(컨셉 ≈40%), 디버그 콘솔 버튼 노출, 컨셉아트 유사도 75%
- 선행 `유니티엔진_씬_질문`으로 두 씬 계층·카메라 값을 조회하고, Work_1 갱신본 `리소스컨셉`의 화면 높이 비율 규격대로 `Scene_Game` 카메라 `orthographicSize`를 교정한다 (바닥·배경 타일 반복 배치와 캐릭터 대비 크기 비율 교정 포함)
- 로비 `Popup_Quit`·게임 `[LogManager]` 인게임 디버그 콘솔 버튼을 플레이 화면에서 숨긴다 (씬 인스턴스 `DebugConsole` 활성 설정)
- 완성 화면 캡처를 씬 컨셉아트(`Concept_Scene_Lobby`·`Concept_Scene_Game`)와 대조해 배치를 맞춘다
- 후속 `유니티엔진_씬_검증` 합격까지 반복한다
- 완료 기준: 검증 합격, 두 씬 저장 `isDirty=false`, 컴파일 에러 0. 사용자가 자율 진행을 지시했으므로 배치 판단은 스스로 확정한다. 막힘은 우회하지 않고 보고한다
